export const COLORS = {
  primary: '#43a047',
  primaryDark: '#2e7d32',
  background: '#1b5e20',
  surface: '#388e3c',
  white: '#fff',
  lightText: '#e8f5e9',
  overlay: 'rgba(0,0,0,0.4)',
  surfaceOverlay: 'rgba(56,142,60,0.9)',
};

export const SPACING = {
  xs: 6,
  sm: 8,
  md: 12,
  lg: 18,
  xl: 24,
  xxl: 40,
};

export const TYPOGRAPHY = {
  title: {
    fontSize: 32,
    fontWeight: 'bold',
  },
  sectionTitle: {
    fontSize: 26,
    fontWeight: 'bold',
  },
  timer: {
    fontSize: 48,
    fontWeight: 'bold',
  },
  body: {
    fontSize: 18,
  },
  button: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  smallButton: {
    fontSize: 18,
    fontWeight: 'bold',
  },
  modalTitle: {
    fontSize: 22,
    fontWeight: 'bold',
  },
  closeButton: {
    fontSize: 16,
    fontWeight: 'bold',
  },
};

export const BORDER_RADIUS = {
  small: 12,
  medium: 16,
  large: 20,
  xl: 24,
  xxl: 30,
};

// Common positioning values
export const POSITIONS = {
  topBar: 40,
  sideMargin: 24,
};

// Common elevation values
export const ELEVATION = {
  low: 2,
  medium: 3,
  high: 4,
};

// Common spacing calculations
export const SPACING_CALC = {
  buttonPadding: SPACING.lg + 2,
  containerPadding: SPACING.lg + 2,
  modalPadding: SPACING.xl + 4,
  modalButtonPadding: SPACING.xl + 8,
}; 